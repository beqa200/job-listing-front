import styled from "styled-components";
import { bgMobile } from "../images";

const BodyWrapper = styled.div`

.bg {
    width: 100%;
    height: 156px;
    background-color: #5CA5A5;
    background-image: url(${bgMobile});
    background-position: 50%;
}
`

export default BodyWrapper;